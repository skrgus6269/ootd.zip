import styled from 'styled-components';

const Layout = styled.div`
  margin: 0px 20px;
  .editMyPageButton {
    position: fixed;
    bottom: 20px;
    width: calc(100% - 40px);
  }
`;

const S = { Layout };

export default S;
