import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  padding: 40px 0px;
  flex-direction: column;
  align-items: center;
  gap: 8px;

  img {
    width: 88px;
    height: 88px;
    border-radius: 50%;
  }
`;

const S = { Layout };

export default S;
