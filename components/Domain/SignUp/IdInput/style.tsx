import styled from 'styled-components';

const Layout = styled.div`
  .title {
    margin-bottom: 8px;
  }
  .helperText {
    margin-top: 8px;
  }
  .hidden {
    visibility: hidden;
  }
`;

const S = {
  Layout,
};

export default S;
