import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  flex-direction: column;
  gap: 4px;
  .truefalse {
    padding-top: 4px;
  }
  .helperText {
    margin-top: 8px;
  }
`;

const S = { Layout };

export default S;
