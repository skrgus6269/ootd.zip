import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  flex-direction: column;
  gap: 24px;
  .helperText {
    margin-top: 16px;
  }
`;

const S = { Layout };

export default S;
