import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  flex-direction: column;
  gap: 4px;
  .helperText {
    margin-top: 4px;
  }
`;

const S = { Layout };

export default S;
