import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  align-items: center;
  text-align: center;
  flex-grow: 1;
  .report {
    color: red;
    flex-grow: 1;
  }
`;

const S = { Layout };

export default S;
