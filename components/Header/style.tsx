import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  width: 390px;
  height: 64px;
  padding: 21px 20px;
  align-items: center;
  gap: 8px;
  flex-shrink: 0;
`;

const S = {
  Layout,
};

export default S;
