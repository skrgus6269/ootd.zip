import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  .splashLogo {
    width: 178px;
  }
`;

const S = { Layout };

export default S;
