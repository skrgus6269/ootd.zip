import styled from 'styled-components';

const Layout = styled.div`
  padding: 0 20px;
  overflow-x: hidden;
  display: flex;
  flex-direction: column;
  gap: 126px;
`;

const S = { Layout };

export default S;
